version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - bodyclose
    - misspell

formatters:
  enable:
    - gofmt
    - goimports
