services:
  hermes:
    build: .
    container_name: hermes
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - hermes_data:/data
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TZ=UTC

volumes:
  hermes_data:
