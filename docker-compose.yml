services:
  hermes:
    image: ghcr.io/infydex/hermes:latest
    container_name: hermes
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - /srv/docker/hermes/data:/data
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TZ=Asia/Kolkata
